backend:
  name: github
  repo: ishchow/artishchow.com
  branch: main

# Required by Sveltia CMS even though we don't use internal media storage.
media_folder: static/uploads
public_folder: /uploads

collections:
  - name: artwork
    label: Artwork
    label_singular: Artwork
    folder: content/artwork
    slug: "{{date | date('YYYYMMDD')}}-d{{day_number}}-{{title}}"
    summary: "{{slug}}"
    create: true
    extension: md
    format: toml-frontmatter
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Description, name: description, widget: string, required: false }
      - { label: "Day Number", name: day_number, widget: string, hint: "e.g. 19 or 16-1" }
      - { label: Date, name: date, widget: datetime, format: "YYYY-MM-DD", date_format: "YYYY-MM-DD", time_format: false }
      - label: Taxonomies
        name: taxonomies
        widget: object
        required: false
        fields:
          - label: Tags
            name: tags
            widget: list
            allow_add: true
            required: false
      - label: Extra
        name: extra
        widget: object
        fields:
          - { label: Thumbnail, name: thumbnail, widget: string, hint: "R2 filename, e.g. 20251119_D4_Kasasagi.jpg" }
      - { label: Body, name: body, widget: markdown }
